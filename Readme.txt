Привет! 

1) В ТЗ про генерацию Model написано: Model и Power генерировать случайным образом (Power - в диапазоне [10; 100),Model - согласно спецификации). 
Я же генерировала так, чтобы создавались (рандомом) буквы от A до Z и цифры от 0 до 9. 
!
Важно отметить, что я намерено сделала так, чтобы могли генерироваться модели, состоящие из 4,5 или 6 символов. Для чего?
Чтобы хотя бы иногда, а если точно, то когда длина строки не равна 5 - вылетало исключение, которое прописали в ТЗ - ""Недопустимая модель <Model>".
!
2) Поэтому при выводе в консоль - будут отображаться все попытки с нужными комментариями.
(  комментарии следующие  - ( если не подходит - то нужное исключение, если все ок - просто как в тз ). 
Логика:
Я заполняю (выволняю попытки сгенерировать подходящую модель) до тех пор, пока полностью не заполнится мой массив.
Его потом (уже заполненный) отправляю на распределение по потокам, чтобы записать итоги в файлы.
Это строго по тз -
ТЗ: 
@
 Информацию о транспортных средствах (возвращаемую методом ToString()) из
сформированного массива записать в текстовые файлы "Cars.txt" и
"MotorBoats.txt" (кодировка utf-16, расположение: папка с файлом решения),
содержащие только сведения о машинах (Cars.txt) и моторных лодках
(MotorBoats.txt) соответственно (информация о каждом транспортном средстве –
с новой строки).
@
3) Повтор реализован. Чтобы завершить нажимаете Esc, иначе любую кнопку, чтобы продолжить.
______________________________________________________________________________
На всякий случай:
Как открыть проект. 
В папке все верно прикреплено. Открываете TransportConcolePeer (это решение) - Там лежит нужный sln- открываете его.
Автоматически откроется библиотека EKRLibrary. Все зависимости установлены. 
Файлы: Файлы лежат там, где sln. Так просили в ТЗ.( ТЗ:  кодировка utf-16, расположение: папка с файлом решения). 
Файлы при возобновлении программы перезаписывается, т.е. запишется последний раз.
А ещё все было написано на Windows.
______________________________________________________________________________
Спасибо за внимание! Удачной проверки!

